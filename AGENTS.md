## Memory
For every substantive completed or interrupted task, before the final response, check
`.codex/memory/MEMORY.md`. If it contains a memory-playbook domain map, run the
`memory-playbook-cx` Closeout protocol and capture gate. Save only verified, reusable project
context, refresh the relevant domain, and never put live status directly in `MEMORY.md`.
`Do not remember` or `temporary` opts the task out.
